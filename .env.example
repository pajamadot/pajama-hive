# Clerk Auth
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

# Neon Postgres (used by Hyperdrive in production, directly in dev)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Anthropic (optional, for plan generation)
ANTHROPIC_API_KEY=sk-ant-...

# Cloudflare (set via wrangler secret in prod)
# HYPERDRIVE connection string is injected via wrangler.toml binding
