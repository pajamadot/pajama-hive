{
  "framework": "nextjs",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @pajamadot/hive-shared build && cd packages/web && next build",
  "outputDirectory": "packages/web/.next"
}
